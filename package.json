{
  "name": "portfolio2",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open",
    "build": "tsc && vite build",
    "preview": "tsc && vite build && vite preview --host --open"
  },
  "typeRoots": [
    "node_modules/@types",
    "src/@types"
  ],
  "devDependencies": {
    "@babylonjs/addons": "^8.11.0",
    "@babylonjs/core": "^8.11.0",
    "typescript": "^5.3.3",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "@babylonjs/havok": "^1.3.10"
  }
}
